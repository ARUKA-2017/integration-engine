version: '3'
services:
  platformer-november-prod-ci:
    container_name: platformer-november-prod-ci
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 80:80
    volumes:
      - .:/usr/share/nginx/html/
      - ./nginx.conf/:/etc/nginx/conf.d/
